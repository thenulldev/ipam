import type {
  CableId,
  ChangeEventId,
  DeviceId,
  DeviceTemplateId,
  DhcpScopeId,
  DnsZoneId,
  FloorplanId,
  ImageId,
  IpAddressId,
  NoteId,
  PortId,
  PrefixId,
  RackId,
  RackReservationId,
  RoomId,
  SiteId,
  TenantId,
  UserId,
  VlanId,
  VrfId,
} from '../types'

// Helper for readable ids (no branding in fixtures; brand on read).
export const id = {
  site: (s: string) => s as SiteId,
  room: (s: string) => s as RoomId,
  floorplan: (s: string) => s as FloorplanId,
  rack: (s: string) => s as RackId,
  device: (s: string) => s as DeviceId,
  port: (s: string) => s as PortId,
  cable: (s: string) => s as CableId,
  vrf: (s: string) => s as VrfId,
  vlan: (s: string) => s as VlanId,
  prefix: (s: string) => s as PrefixId,
  ip: (s: string) => s as IpAddressId,
  tenant: (s: string) => s as TenantId,
  user: (s: string) => s as UserId,
  note: (s: string) => s as NoteId,
  image: (s: string) => s as ImageId,
  template: (s: string) => s as DeviceTemplateId,
  change: (s: string) => s as ChangeEventId,
  reservation: (s: string) => s as RackReservationId,
  dhcp: (s: string) => s as DhcpScopeId,
  dns: (s: string) => s as DnsZoneId,
}
